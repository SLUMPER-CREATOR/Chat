# 🤝 Руководство по участию в разработке

Спасибо за интерес к участию в развитии Django Chat Application! Мы приветствуем любые вклады в проект.

## 📋 Как участвовать

### 🐛 Сообщение об ошибках
1. Проверьте, что ошибка еще не была зарегистрирована в [Issues](https://github.com/yourusername/django-chat/issues)
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Информация о системе (OS, Python версия, браузер)

### 💡 Предложение новых функций
1. Создайте Issue с тегом "enhancement"
2. Опишите предлагаемую функцию
3. Объясните, почему она будет полезна
4. Приложите mockups или диаграммы (если есть)

### 🔧 Внесение изменений в код

#### Настройка окружения разработки
```bash
# 1. Fork репозитория на GitHub
# 2. Клонируйте ваш fork
git clone https://github.com/yourusername/django-chat.git
cd django-chat

# 3. Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# 4. Установите зависимости для разработки
pip install -r requirements.txt
pip install -r requirements-dev.txt  # если есть

# 5. Настройте pre-commit hooks
pre-commit install
```

#### Процесс разработки
1. **Создайте ветку для новой функции:**
   ```bash
   git checkout -b feature/amazing-feature
   ```

2. **Следуйте стандартам кодирования:**
   - PEP 8 для Python кода
   - Используйте осмысленные имена переменных и функций
   - Добавляйте docstrings для функций и классов
   - Комментируйте сложную логику

3. **Тестирование:**
   ```bash
   # Запустите тесты
   python manage.py test
   
   # Проверьте покрытие
   coverage run --source='.' manage.py test
   coverage report
   ```

4. **Commit изменений:**
   ```bash
   git add .
   git commit -m "feat: добавить новую функцию"
   ```

5. **Push и создание PR:**
   ```bash
   git push origin feature/amazing-feature
   ```

## 📝 Стандарты кодирования

### Python код
```python
# Хорошо
def get_user_messages(user_id: int) -> QuerySet:
    """
    Получить все сообщения пользователя.
    
    Args:
        user_id: ID пользователя
        
    Returns:
        QuerySet с сообщениями пользователя
    """
    return Message.objects.filter(user_id=user_id).order_by('-timestamp')

# Плохо
def get_msgs(uid):
    return Message.objects.filter(user_id=uid)
```

### JavaScript код
```javascript
// Хорошо
function sendMessage(messageContent) {
    if (!messageContent.trim()) {
        return;
    }
    
    socket.send(JSON.stringify({
        type: 'message',
        content: messageContent
    }));
}

// Плохо
function send(msg) {
    socket.send(JSON.stringify({t: 'msg', c: msg}));
}
```

### HTML/CSS
- Используйте семантические HTML теги
- Следуйте принципам доступности (a11y)
- Используйте TailwindCSS классы
- Обеспечьте адаптивность

## 🧪 Тестирование

### Типы тестов
1. **Unit тесты** - для отдельных функций и методов
2. **Integration тесты** - для взаимодействия компонентов
3. **WebSocket тесты** - для проверки real-time функций

### Пример теста
```python
from django.test import TestCase
from django.contrib.auth.models import User
from chat.models import Message

class MessageModelTest(TestCase):
    def setUp(self):
        self.user = User.objects.create_user(
            username='testuser',
            password='testpass123'
        )
    
    def test_message_creation(self):
        message = Message.objects.create(
            user=self.user,
            content='Test message'
        )
        self.assertEqual(message.user, self.user)
        self.assertEqual(message.content, 'Test message')
        self.assertIsNotNone(message.timestamp)
```

## 📚 Документация

### Обновление документации
- Обновляйте README.md при добавлении новых функций
- Добавляйте docstrings к новым функциям
- Обновляйте API документацию
- Создавайте примеры использования

### Комментарии в коде
```python
# Хорошо
def calculate_user_rating(user):
    """
    Вычисляет рейтинг пользователя на основе активности.
    
    Рейтинг учитывает:
    - Количество сообщений
    - Количество полученных лайков
    - Время участия в сообществе
    """
    messages_count = user.message_set.count()
    likes_count = user.received_likes.count()
    # ... логика вычисления
```

## 🎯 Приоритетные области для вклада

### 🔥 Высокий приоритет
- Исправление критических ошибок
- Улучшение производительности
- Безопасность
- Мобильная адаптивность

### 📈 Средний приоритет
- Новые функции чата
- UI/UX улучшения
- Дополнительные тесты
- Оптимизация базы данных

### 💡 Идеи для новых функций
- Поддержка файлов и изображений в сообщениях
- Эмодзи реакции на сообщения
- Поиск по сообщениям
- Темная тема
- Мобильное приложение
- Интеграция с внешними сервисами

## 🔍 Code Review

### Что мы проверяем
- Соответствие стандартам кодирования
- Наличие тестов
- Производительность
- Безопасность
- Документация

### Процесс review
1. Автоматические проверки (CI/CD)
2. Ручной review от мейнтейнеров
3. Обсуждение и исправления
4. Merge в main ветку

## 🏷️ Соглашения о коммитах

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить поддержку эмодзи в сообщениях
fix: исправить ошибку отправки пустых сообщений
docs: обновить README с новыми функциями
style: исправить форматирование в chat/views.py
refactor: оптимизировать запросы к базе данных
test: добавить тесты для системы лайков
chore: обновить зависимости
```

## 🎉 Признание вклада

Все участники будут упомянуты в:
- README.md в разделе Contributors
- CHANGELOG.md для значительных вкладов
- Release notes

## 📞 Связь

- **GitHub Issues** - для багов и предложений
- **GitHub Discussions** - для общих вопросов
- **Email** - your.email@example.com для приватных вопросов

## 📖 Дополнительные ресурсы

- [Django документация](https://docs.djangoproject.com/)
- [Django Channels документация](https://channels.readthedocs.io/)
- [TailwindCSS документация](https://tailwindcss.com/docs)
- [WebSocket API](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket)

---

**Спасибо за ваш вклад в развитие проекта! 🚀**